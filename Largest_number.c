#include <stdio.h>  int main() {     int n, i, largest;      printf("Kitne numbers enter karoge: ");     scanf("%d", &n);      int arr[n];      printf("Numbers enter karo:\n");     for(i = 0; i < n; i++) {         scanf("%d", &arr[i]);     }      largest = arr[0];      for(i = 1; i < n; i++) {         if(arr[i] > largest) {             largest = arr[i];         }     }      printf("Largest number = %d\n", largest);      return 0; }largest
